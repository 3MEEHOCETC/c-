если пк дает x и x != y -> why? x > y? 1.
1. 0-(x-1) - первоначсально 0-100

нужно чтобы комп угадал загаданное пользователем число
как это реализовать?
сначала определим диапазон чисел: 0-100 // уже внесли ясность
значит комп должен угадать какое число юзер загадал + подсчитать количество попыток своих

1. Загадайте число
2. Ваше число х? 
3. Ответы: Да, меньше/больше // так даем 3 варианта чтобы комп не делал 100 попыток 
// меньше/больше даст ему ориентир - если число 50, то он отсечет все что выше/меньше 50 - диапазон станет 0-50 (50-100)
4. Каждую попытку комп должен засчитывать - tries ++i;
//рашн хакеры знали англ или что? 

#include <iostream>
#include <cstdlib>
#include <ctime>

int main() 
int guess = x;
int y = secretNumber;
{
srand(static_cast<unsigned int>time(0))); // генератор чисел
//а зачем компу генератор чисел? он должен угадать мое число
//буквально сделать так -
//0-100 - потом проверить то ли число я загадал
//если нет - сделать отсев
//тогда что делать? все же рандомайзер числе нужен - он должен перечислять набор чисел 
//чтобы попасть, каждая наводка приближает его к ответу


do 
{
std::cout << "You should choice a number and I'll guess it!";
std::cin >> Got it! >> endl;


if (x > y) << 
